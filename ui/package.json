{
  "name": "jobrunner-ui",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run src/server.ts",
    "build": "bun build src/main.ts --outdir=dist --minify --sourcemap=external",
    "preview": "bun run src/server.ts --production",
    "typecheck": "bunx tsc --noEmit"
  },
  "dependencies": {
    "@rhds/elements": "^4.0.0",
    "@rhds/icons": "^2.0.0",
    "@rhds/tokens": "^3.0.0",
    "elkjs": "^0.9.3",
    "lit": "^3.2.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}

